# Mapa de Proyecto: Control de Equipos 3.0

## 1. Arquitectura
- **Patrón**: Monolito por capas con API REST + SPA
- **Frontend**: React 19 SPA con Vite 7, Tailwind v4, React Router v7
- **Backend**: Express 4 monolítico en `backend/`, sin separación por microservicios. Nodemon con hot-reload — el servidor se reinicia solo al editar cualquier archivo.
- **Base de datos**: SQLite local vía raw sqlite3 (`db/database.js`). Prisma completamente eliminado.
- **Comunicación**: HTTP/JSON + Socket.IO para notificaciones en tiempo real
- **External services**: Google Drive (backups), Nodemailer/Gmail (email), web-push (notificaciones PWA)
- **Auth**: JWT en httpOnly cookie + refresh token rotation

## 2. Stack Crítico
- **Node.js**: >=22.0.0
- **Backend**: Express ^4.18.2, sqlite3 ^5.1.7 (raw), JWT (jsonwebtoken + bcryptjs + zod), Socket.IO ^4.8.3, **TypeScript** (tsc + tsx)
- **Logging**: **Pino** + pino-http con redact de secrets, LOG_LEVEL env-configurable
- **Rate limiting**: **express-rate-limit** con 8 limitadores específicos por endpoint
- **Frontend**: React ^19.2.0, Vite ^7.3.1, Tailwind CSS ^4.2.0, Framer Motion ^12.34.3, Recharts ^3.7.0, **TypeScript** (.ts/.tsx, ESM)
- **Frontend tests**: **Vitest** + **React Testing Library** (13 tests)
- **DB**: `sqlite3` ^5.1.7 (raw, driver único). Prisma eliminado completamente.
- **PWA**: Service Worker custom + offline fallback + manifest + push notifications vía web-push

## 3. Convenciones Detectadas
- **Backend**: **TypeScript** (.ts) con CommonJS (`require`/`module.exports`), estructura Routes → Controllers → Services → Repositories. TypeScript no-estricto para migración pragmática. Directorios en inglés (`services/`, `repositories/`, `sync/`).
- **Frontend**: **TypeScript** (.ts/.tsx) con ESM (`import/export`), lazy loading de páginas
- **Auth**: AuthContext de React (useAuth) en frontend, httpOnly cookie + refresh token rotation en backend
- **Nombrado**: Archivos backend en español (`equipos.controller.ts`, `soporte.service.ts`), variables en inglés
- **CSS**: Tailwind v4 con `@import "tailwindcss"`, glassmorphism, tema oscuro, indigo como color primario
- **Commits**: Conventional Commits desde sesión de remediación
- **Logging**: Pino con `redact` para cookies/passwords/tokens. HTTP logging automático vía pino-http. Nivel configurable via `LOG_LEVEL` env.
- **Rate limiting**: 8 limitadores con ventanas y thresholds específicos por operación, factory `createLimiter()` + logging en 429

## 4. Puntos de Dolor (Deuda Técnica)

### Alto Riesgo
1. ~~Frontend sin coverage completo~~ ✅ 57 tests (11 suites) cubren api, Toast, Login, Dashboard, AuthContext, ToastContext, CommonCard, ConfirmModal, SearchInput, Select, NotificationBell

### Medio Riesgo
2. **Secrets aún en .env en disco**: JWT_SECRET y ADMIN_PASSWORD rotados (64-byte hex + random). Google/Gemini credenciales aún en .env (necesarias para funcionamiento).
3. **Sin healthcheck de logging**: No hay alertas configuradas si Pino falla o logs se pierden.

### Bajo Riesgo
8. **Node >=22**: Requisito muy reciente, posible incompatibilidad en CI runners antiguos.
9. **`.env.example` solo en backend/**: Scripts raíz dependen de vars de entorno no documentadas a nivel raíz.

### Resuelto en Sesiones previas
- ~~Dual DB access path~~ (Sesión 3)
- ~~Backend sin tests~~ ✅ Backend: Vitest + 11 tests (Sesión 7)
- ~~Sin Docker/CI/CD~~ ✅ Docker + GH Actions (Sesión 7)
- ~~Auth faltante en endpoints~~ ✅ Todos protegidos (Sesión 6)
- ~~Backend sin TypeScript~~ ✅ 44+ archivos .ts (Sesión 8)
- ~~Logging estructurado~~ ✅ Pino (Sesión 8)
- ~~Rate limiting parcial~~ ✅ 8 limitadores (Sesión 8)
- ~~Frontend sin tests~~ ✅ 13 tests (Sesión 8)
- ~~Backend sin linter~~ ✅ ESLint + TS (Sesión 8)
- ~~Dead code~~ ✅ 7 archivos (Sesión 8)
- ~~Paginación~~ ✅ equipos + historial (Sesión 8)
- ~~calcularHashEquipo~~ ✅ Phantom fields (Sesión 8)
- ~~Frontend sin TypeScript~~ ✅ 51+ archivos .ts/.tsx (Sesión 9)
- ~~N+1 query~~ ✅ Batch especificaciones (Sesión 9)
- ~~Secrets débiles~~ ✅ JWT + ADMIN_PASSWORD rotados (Sesión 9)
- ~~Mezcla español/inglés~~ ✅ `servicios/`→`services/`, `repositorios/`→`repositories/`, `sincronizacion/`→`sync/` (Sesión 9)
- ~~Frontend coverage parcial~~ ✅ 57 tests (11 suites) cubren contexts, components, pages (Sesión 10)

## 5. Historial de Correcciones Aplicadas

### Sesión 1 (2026-05-24) — Fase 1+2
- Seguridad: JWT fallbacks eliminados, Math.random → crypto.randomInt, rate limiting en password reset
- Sync engine: Hash pipeline reconectado, lock con TTL+heartbeat, double-fetch eliminado, backup path corregido, sync_logs creada
- Transacciones: WAL mode + busy_timeout + transacciones en writes críticos
- Auth: JWT migrado de localStorage a httpOnly cookie

### Sesión 2 (2026-05-24) — Fase 3 + Limpieza
- Hardening backend: Zod validation, refresh token rotation, graceful shutdown, rate login 10/15min, body limit 1MB
- Frontend: AuthContext, focus trap + ARIA en modales, offline fallback en SW, lazy socket.io-client
- Limpieza: Railway/PostgreSQL eliminado, 6 scripts muertos removidos, deps muertas (pg, sqlite, clsx, tailwind-merge, localforage), modelo duplicado tareas_soporte eliminado de Prisma, .env.example limpiado

### Sesión 3 (2026-05-26) — Migración Prisma completa + CSP hardening + merge + fixes finales
- **Master** (`aef2b15`): Migración COMPLETA de los 5 servicios (config, comunicaciones, reporting, analitica, mensajes) + eliminación de `prismaClient.js`, `schema.prisma`, `@prisma/client`
- Fixes CSP (`118032b`): CSP nonce cargado al startup, `injectNonces()` reutilizable, `connectSrc` con `fonts.gstatic.com`, ruta `/sw.js` sin CSP headers
- Rama `recuperacion-codigo` mergeada a master (fast-forward) + fix CORS + auth + Zod commit (`a89d40e`):
  - CORS: eliminar bypass `!IS_PROD` (cualquier origen en desarrollo). Socket.IO CORS con lista fija
  - Zod: `.errors` → `.issues` en `validate()` middleware — corregía 500 en toda validación fallida
  - Auth middleware: agregado a `/api/dashboard/summary` y `/api/config/*` (4 endpoints públicos sin auth)
  - Login: validación de campos vacíos antes de consultar DB

### Sesión 4 (2026-05-26) — Lighthouse Performance: 56→85+ (build optimizations)
- **Favicon**: Reducido de 2.3MB (2048x2048) a 4.6KB — 99.8% reduction. Nuevos: 32x32 PNG + WebP favicon, 192x512 PWA icons (PNG+WebP), OG image 1200x630
- **Manual chunks**: `vendor-anim` para framer-motion (128KB raw / 42KB gzip) y `vendor-charts` para recharts (317KB raw / 97KB gzip) — ahora cacheables por separado
- **ToastContext/Toast**: Eliminada dependencia eager de framer-motion → reemplazada por CSS keyframes (slide-right + progress bar). Main bundle reducido 116KB
- **Main bundle raw**: 348KB → 232KB (main). Gzip: 114KB → 75KB
- **Lighthouse** (confirmado con reports JSON en Desktop):
  - LCP: 15.4s (score 0) → 2.9s (score 0.8) — **81% mejora**
  - TBT: 580ms (score 0.51) → 10ms (score 1) — **98% mejora**
  - TTI: 15.5s (score 0.07) → 3.0s (score 0.95) — **80% mejora**
  - Total page weight: 2.6MB → 415KB — **84% reducción**
   - Acc/BestPractices/SEO: 100/100 mantenido

### Sesión 5 (2026-05-26) — Lighthouse Score 100 (rama `feature/lighthouse-optimization`)
- **Code splitting granular**: Main entry 356KB → 11KB al separar `lucide-react` (20KB), `react-router-dom` (35KB), `socket.io-client` (41KB), `date-fns` (26KB) en chunks propios
- **Brotli pre-compression**: Express middleware sirve assets `.br` pre-comprimidos con MIME types correctos (`application/javascript`, `text/css`)
- **Cache headers**: `Cache-Control: public, max-age=31536000, immutable` para assets hasheados, `no-cache` para HTML/manifest
- **Font preload directo**: Preload del woff2 con `fetchpriority="high"` + crossorigin
- **Lighthouse 100/100** confirmado:
  - FCP: 0.5s (score 1) | LCP: 0.7s (score 0.99)
  - TBT: 0ms (score 1) | TTI: 0.7s (score 1)
  - Page weight: 277KB | Requests: 18 | Main thread: 0.3s

### Sesión 6 (2026-05-26) — Fix críticos runtime + Auth endpoints + React Doctor (ramas `feature/fix-critical-bugs`, merge `da9089a` y `2460ea7`)
- **5 bugs críticos runtime resueltos**:
  - Instalado `uuid` (ipam.service.js requería pero no estaba en package.json — crash al crear redes)
  - Creada tabla `especificaciones_repuestos` en `initializeTables()` (componentes.service la consultaba — 500 error)
  - Agregadas columnas `usuario_id`, `remitente`, `leido` a `mensajes_admin` (comunicaciones.service insertaba en columnas inexistentes)
  - `syncStats.js`: reemplazado campo muerto `pendientesFaseRemoteLocal` por `eliminados` (NaN en reports)
  - SQL injection: template literal en INSERT OR REPLACE sync_metadata → parámetro bind
- **SCHEMA_VERSION bump 5→6**
- **Auth middleware agregado** a 15 endpoints sin protección: equipos GET, componentes GET, historial GET, soporte (todo), ipam (todo), prestamos (todo), mantenimiento (todo)
- **mantenimiento.controller.js**: reemplazados console.error+res.status(500) por next(err) en 5 catch blocks
- **React Doctor fixes**: ProtectedRoute/RoleRoute/IndexRedirect a module scope en App.jsx, InputError a module scope en EquipoFormModal.jsx, socket.off() cleanup en NotificationBell.jsx
- **Splash logo**: favicon.png (32x32px, borroso) → icon-192x192.png (192x192px) en index.html

### Sesión 7 (2026-05-26) — Tests + Secrets + Docker/CI/CD (rama `feature/vitest-secrets-docker`, merge `0191063`)
- **Vitest + 11 smoke tests** (backend):
  - app.js/server.js refactored: Express app exportable desde app.js, server.js solo importa + listen()
  - auth.test.js: 7 tests (login válido/inválido/vacío, /me con/sin cookie, logout)
  - equipos.test.js: 4 tests (listar sin auth→401, listar con auth→200, by-id→404, by-id sin auth→401)
  - vitest.config.js + setup.js + helpers.js
- **Secrets hardening**:
  - .env.example: placeholders reemplazados por instrucciones de generación con crypto.randomBytes()
  - server.js: validación startup — warn si JWT_SECRET < 32 chars o ADMIN_PASSWORD es admin123
  - Recovery codes hasheados con SHA-256 antes de guardar en DB (plaintext nunca persiste)
  - crypto.timingSafeEqual() para comparación de recovery codes (previene timing attack)
  - Catch block vacío de logout corregido (console.error agregado)
- **Docker + CI/CD**:
  - Dockerfile multi-stage: build frontend con node:22-alpine → servir con node:22-alpine, non-root user, HEALTHCHECK
  - docker-compose.yml: puerto 3000, volumen persistente SQLite, env_file, restart unless-stopped
  - .github/workflows/ci.yml: 4 jobs paralelos (lint-frontend, test-backend, build-frontend, docker-build)
  - .dockerignore (root + backend)
- **SCHEMA_VERSION bump 6→7**, columnas: permisos_json en usuarios, grado/grado_id en responsables, ubicacion en ubicaciones

### Sesión 8 (2026-05-31) — Saldar deuda técnica: TypeScript, Logging, Tests, Linter, Rate Limiting, Paginación

**ALTA Prioridad (4/4 completados):**
- **TypeScript backend**: Migración completa de ~44 archivos `.js` → `.ts` (CommonJS). tsconfig.json no-estricto, `tsx` como runtime. Dockerfile actualizado con compilación TS. ✅ `tsc --noEmit` 0 errors, 11/11 tests pasan
- **Logging estructurado (Pino)**: ~73 console.log/error/warn reemplazados. `backend/utils/logger.ts` con `redact` para cookies/passwords/tokens. Middleware `pino-http` en app.ts con `autoLogging.ignore` para `/health`. Nivel configurable via `LOG_LEVEL` env
- **Rate limiting completo**: 8 limitadores específicos creados en `backend/utils/rateLimiter.ts` con factory `createLimiter()` + logging en 429. Aplicados a: login (10/15min), password-reset (3/15min), register (5/hora), backup/sync (10/30min), export (20/15min), IPAM mutations (50/15min), usuario writes (30/15min), global API (2000/15min)
- **Frontend tests**: Vitest + React Testing Library + jsdom + user-event. 13 tests en 3 suites: api.test.js (6 tests: timeout, offline, GET, POST, 401, 403), Toast.test.jsx (4 tests: render, close, auto-dismiss, icon types), Login.test.jsx (3 tests: form render, empty validation, submit). ✅ 13/13 pasan

**MEDIA Prioridad (5/5 completados):**
- **Backend linter**: `backend/eslint.config.js` flat config con `@eslint/js` + `typescript-eslint`. Reglas: `no-console: warn`, `no-explicit-any: off`, `no-require-imports: off`. ✅ 0 errors, 32 warnings pre-existentes
- **Dead code removal**: 7 archivos eliminados: `Inventario.jsx`, `App.css`, `GestionUbicaciones.jsx`, `GestionGrados.jsx`, `GestionEstados.jsx`, `EquipoDeleteModal.jsx`, `SoporteDeleteModal.jsx`
- **Paginación**: equipos + historial con `?page=1&limit=50` (defaults: page=1, limit=50, max=100). COUNT + LIMIT/OFFSET en queries. Respuesta: `{ data: [...], pagination: { page, limit, total, totalPages } }`
- **calcularHashEquipo.ts**: Field names corregidos. Eliminados phantom fields (nombre, descripcion, numero_serie, modelo, marca). Hash ahora refleja solo columnas reales: id, ine, nne, serie, categoria_id, estado_id, ubicacion_id, responsable_id, is_deleted
- **Reporte naming**: `backend/docs/naming-convention-report.md` creado con análisis completo y recomendaciones

### Sesión 9 (2026-05-31) — TypeScript frontend + N+1 query + Secrets + Naming
- **Frontend TypeScript**: 51 archivos `.js`/`.jsx` → `.ts`/`.tsx` (ESM). tsconfig.json no-estricto, vite + vitest configs migrados a `.ts`. ✅ `npx vite build` 0 errors, 13/13 frontend tests pasan
- **N+1 query fix**: Equipment listing: 51 queries/página → 2 queries (batch `SELECT ... WHERE equipo_id IN (...)`). Eliminadas ~49 queries por página en equipos service
- **Secrets rotation**: JWT_SECRET rotado a 64-byte hex (`crypto.randomBytes(64).toString('hex')`). ADMIN_PASSWORD rotado de `admin123` a random 12-char. VAPID keys preservadas. Server startup validation: `process.exit(1)` si JWT_SECRET < 32 chars o ADMIN_PASSWORD es `admin123`/< 8 chars
- **Naming unificado**: `servicios/` → `services/`, `repositorios/` → `repositories/`, `sincronizacion/` → `sync/`. 18 archivos con imports actualizados. ✅ tsc 0 errors, 11/11 tests pasan

### Sesión 11 (2026-05-31) — Frontend TypeScript: ~130 type errors → 0 ✅
- **vite-env.d.ts**: tipos Vite client, declaraciones `.webp`/`.css`, `Navigator.standalone`
- **Component types**: interfaces explícitas para ConfirmModal, CommonCard, Select, SearchInput con props opcionales
- **Test mocks**: framer-motion mocks simplificados (sin forwardRef, cast tag as string), `global.fetch` → `globalThis.fetch`
- **Source fixes**: notificationManager class property, api.ts options typed, LocationChart cornerRadius removido, EquipoFormModal/SoporteFormModal estados tipados, Login.tsx ref React 19
- **Total**: 22 archivos modificados. ✅ `tsc --noEmit` 0 errors, vite build OK, 57/57 tests pasan

## 6. Estado de Deuda Técnica

### Resuelto en Sesiones 1-8
- ~~Dual DB access path~~ (Sesión 3)
- ~~Sync engine hash/lock/transacciones~~ (Sesión 1)
- ~~JWT localStorage→cookie~~ (Sesión 1)
- ~~Auth en endpoints públicos~~ ✅ Requieren verificarAutenticacion (Sesión 6)
- ~~Secrets débiles~~ ✅ Validación startup + timingSafeEqual (Sesión 7)
- ~~Backend sin tests~~ ✅ Backend: Vitest + 11 tests (Sesión 7)
- ~~Sin Docker/CI/CD~~ ✅ Docker multi-stage + GH Actions (Sesión 7)
- ~~Backend sin TypeScript~~ ✅ Migrado .ts (Sesión 8)
- ~~Logging estructurado~~ ✅ Pino (Sesión 8)
- ~~Rate limiting parcial~~ ✅ 8 limitadores (Sesión 8)
- ~~Frontend sin tests~~ ✅ 13 tests (Sesión 8)
- ~~Backend sin linter~~ ✅ ESLint + TS (Sesión 8)
- ~~Dead code~~ ✅ 7 archivos eliminados (Sesión 8)
- ~~Paginación ausente~~ ✅ equipos + historial (Sesión 8)
- ~~calcularHashEquipo~~ ✅ Field names fixed (Sesión 8)

### Sin deuda técnica de alto/medio riesgo pendiente
Todo resuelto en Sesiones 8-9. Pendientes menores no bloqueantes: Google/Gemini credenciales en .env (necesarias runtime), healthcheck de logging (alertas si Pino falla), frontend E2E tests con Playwright (próxima fase).

## 7. Infraestructura Agregada

| Componente | Archivos | Estado |
|------------|----------|--------|
| Tests backend | `backend/vitest.config.js`, `backend/src/tests/*.ts` | ✅ 11 tests |
| Tests frontend | `frontend/src/tests/` (11 suites) | ✅ 57 tests (api, Toast, Login, Dashboard, AuthContext, ToastContext, CommonCard, ConfirmModal, SearchInput, Select, NotificationBell) |
| TypeScript backend | `backend/tsconfig.json`, `backend/**/*.ts` (44+ archivos) | ✅ tsc --noEmit 0 errors |
| TypeScript frontend | `frontend/tsconfig.json`, `frontend/src/**/*.ts` (51+ archivos) | ✅ tsc --noEmit 0 errors, vite build OK |
| Logging | `backend/utils/logger.ts`, pino-http en app.ts | ✅ Pino + redact |
| Rate limiting | `backend/utils/rateLimiter.ts`, 8 limitadores en rutas | ✅ Todos críticos |
| Linter backend | `backend/eslint.config.js` | ✅ 0 errors, 32 warnings |
| Docker | `Dockerfile`, `docker-compose.yml`, `.dockerignore` | ✅ Multi-stage + healthcheck (TS-aware) |
| CI/CD | `.github/workflows/ci.yml` | ✅ GitHub Actions (+ typecheck step) |
| Secrets hardening | `backend/.env.example`, `backend/server.ts`, `backend/controllers/auth.controller.ts` | ✅ jwt + admin rotados, startup validation |
| Auth endpoints | 7 route files | ✅ Todos protegidos |
| Paginación | equipos.controller.ts, historial.controller.ts, services/ | ✅ page/limit defaults 1/50 |
| Hash sync | `backend/services/calcularHashEquipo.ts` | ✅ Field names corregidos |
| N+1 fix | `backend/services/equipos.service.ts` | ✅ Batch especificaciones |
| Naming unificado | `services/`, `repositories/`, `sync/` (en inglés) | ✅ Directorios renombrados |

## 8. Dominio y Propósito
Sistema institucional de gestión de inventario de equipos IT con:
- Ciclo de vida de equipos (alta, asignación, seguimiento, baja)
- Sistema de tickets de soporte con estimación de costos
- Inventario de repuestos con tracking de movimientos
- Gestión de préstamos de equipos
- IPAM (IP Address Management) con segmentación de red y VLANs
- Historial de asignación de personal
- Exportación a Excel, backups a Google Drive
- Sincronización multi-instancia con soporte offline
- Notificaciones push PWA + offline fallback
- Recuperación de contraseña vía email
