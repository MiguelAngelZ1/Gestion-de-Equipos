/**
 * Imprime el resumen final de la sincronización (SILENCIADO)
 */
function imprimirResumenSync(stats, totalLocal, totalRemote, detalles = []) {
  // Console logs removed as per request
}

module.exports = {
  imprimirResumenSync,
};
